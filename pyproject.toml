[project]
name = "harness-from-scratch"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=3.16.2",
    "python-dotenv>=1.2.3",
    "scikit-learn>=1.9.1",
    "wandb>=0.30.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
